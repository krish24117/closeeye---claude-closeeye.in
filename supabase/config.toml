project_id = "kghwmiriboavmyswcqnr"

[functions.razorpay-webhook]
verify_jwt = false

[functions.send-visit-reminder]
verify_jwt = false

[functions.send-monthly-summary]
verify_jwt = false

[functions.care-intelligence-scan]
verify_jwt = false

# Public endpoints — no JWT required
# NB: ask-health-public was deleted 2026-07-17. It was Tier 0 — unauthenticated medical
# guidance — built before /connect became the deterministic engine, and nothing in the
# product had called it for some time. It stayed deployed and open, answering medical
# questions from anyone with the URL. Superseded, so removed rather than maintained.
[functions.waitlist-signup]
verify_jwt = false

# Booking requests accept optional auth (guests may book)
[functions.submit-booking-request]
verify_jwt = false

# Status lifecycle
[functions.update-booking-status]
verify_jwt = true

# Cron — called by pg_cron / Supabase scheduled jobs with service-role key
[functions.check-overdue-bookings]
verify_jwt = false

# Cron — 7am IST founder daily briefing push
[functions.founder-daily-briefing]
verify_jwt = false

# Cron — daily WhatsApp nurture sequence for un-converted registrants
[functions.founder-nurture-sequences]
verify_jwt = false

# Public forms — submitted from unauthenticated marketing pages, so NO JWT. Recorded here
# explicitly so a redeploy never flips them to jwt-required (the CLI defaults to true for any
# function missing from this file), which would silently break the public forms.
[functions.submit-consultation-request]
verify_jwt = false

[functions.submit-custom-care-request]
verify_jwt = false

[functions.survey-submit]
verify_jwt = false
