/** /space/book — the in-app Book-a-Visit flow, re-homed from /family/book (IA consolidation).
 *  Re-export, not a copy: one source of truth, zero logic change; /family/book 308s here. */
export { default } from '@/app/family/book/page'
