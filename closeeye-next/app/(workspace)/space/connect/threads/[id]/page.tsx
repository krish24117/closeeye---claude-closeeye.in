/** /space/connect/threads/[id] — the per-member conversation (PM thread + Ask history),
 *  re-homed from /family/connect/[id] (IA consolidation). Re-export: one source, zero
 *  messaging-logic change; the legacy path 308s here. */
export { default } from '@/app/family/connect/[id]/page'
