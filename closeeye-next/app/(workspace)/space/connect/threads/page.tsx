/** /space/connect/threads — conversations home (PM threads + care updates + per-member Ask
 *  history), re-homed from /family/connect (IA consolidation). Re-export: one source, zero
 *  messaging-logic change; /family/connect 308s here. */
export { default } from '@/app/family/connect/page'
