/** /space/activity/visit/[id] — the full visit report, re-homed from /family/visits/[id]
 *  (IA consolidation). Re-export: one source, zero logic change; the legacy path 308s here. */
export { default } from '@/app/family/visits/[id]/page'
