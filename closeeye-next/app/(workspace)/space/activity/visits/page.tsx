/** /space/activity/visits — the visit list, re-homed from /family/visits (IA consolidation).
 *  Re-export: one source, zero booking/payment-logic change; /family/visits 308s here. */
export { default } from '@/app/family/visits/page'
