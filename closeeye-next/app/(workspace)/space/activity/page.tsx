import { WorkspacePlaceholder } from '@/components/workspace/workspace-placeholder'

export default function ActivityPage() {
  return <WorkspacePlaceholder title="Activity" sub="What changed, presence stories, the family's story. Built in Sprint 4." />
}
