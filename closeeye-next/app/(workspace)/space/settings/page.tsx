import { WorkspacePlaceholder } from '@/components/workspace/workspace-placeholder'

export default function SettingsPage() {
  return <WorkspacePlaceholder title="Settings" sub="Profile, preferences, and Trust & Safety. Migrated from /family/profile in a later sprint." />
}
